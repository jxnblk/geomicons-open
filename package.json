{
  "name": "geomicons-open",
  "version": "3.0.0-beta.2",
  "description": "Open Source Icons for the Web",
  "main": "src/index.js",
  "scripts": {
    "build": "mkdir -p dist/svg && node build-svg.js",
    "test": "standard && ava -v",
    "start": ""
  },
  "devDependencies": {
    "ava": "^0.15.2",
    "standard": "^7.1.2"
  }
}
